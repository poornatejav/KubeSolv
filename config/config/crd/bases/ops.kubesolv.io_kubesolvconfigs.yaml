---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: kubesolvconfigs.ops.kubesolv.io
spec:
  group: ops.kubesolv.io
  names:
    kind: KubeSolvConfig
    listKind: KubeSolvConfigList
    plural: kubesolvconfigs
    singular: kubesolvconfig
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: KubeSolvConfig is the Schema for the kubesolvconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KubeSolvConfigSpec defines the desired state of KubeSolv
            properties:
              mode:
                default: monitor
                description: 'Mode: "monitor" (passive) or "auto-heal" (active)'
                enum:
                - monitor
                - auto-heal
                type: string
              targetNamespaces:
                description: |-
                  TargetNamespaces: List of namespaces to watch (e.g., ["default", "production"])
                  If empty, it watches ALL namespaces.
                items:
                  type: string
                type: array
            type: object
          status:
            description: KubeSolvConfigStatus defines the observed state
            properties:
              health:
                type: string
              incidentCount:
                type: integer
              lastScanTime:
                format: date-time
                type: string
            required:
            - health
            - incidentCount
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
